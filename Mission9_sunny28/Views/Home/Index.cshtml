@{
    ViewData["Title"] = "Book List";
}

@model BooksViewModel

@*Copy in the bootstrap for the page*@
@foreach (Book b in Model.Books)
{
    <div class="card card-outline-primary m-1 p-1">
        <div class="bg-faded p-1">
            <h4>
                @*Include all fields but the bookid*@
                @b.Title
                <span class="badge badge-pill badge-primary" style="float:right; color:black;">
                    <small>@b.Price.ToString("c")</small>
                </span>
            </h4>
            <div class="card-text p-1">@b.Author
                <span class="badge badge-pill badge-primary" style="float:right; color:black;">
                    <small>@b.Publisher</small>
                </span>
            </div>
        </div>
        <div class="card-text p-1">
            @b.Category  @b.Classification (@b.PageCount page read)
            <span class="badge badge-pill badge-primary" style="float:right; color:black;">
                <small>ISBN @b.Isbn.ToString()</small>
            </span>
        </div>
    </div>
}
<div page-model="@Model.PageInfo" page-action="Index" page-classes-enabled="true"
     page-class="btn" page-class-normal="btn-outline-dark"
     page-class-selected="btn-primary" class="btn-group pull-right m-1">
</div>